const input = document.querySelector('#name-input');
const output = document.querySelector('#name-output');

// input.addEventListener('input', (input,output) =>  output.textContent=input.textContent )
console.dir(output);